version: '3'
services:
  calculator:
    image: edyta/calculator
    build: .
    container_name: mycontainer
    ports:
      - "8080:8080"
    volumes:
      - ./var/log/calculator:/var/log/calculator

    logging:
      driver: json-file